# Cloudflare Pages static headers (copied to dist via public/)
# https://developers.cloudflare.com/pages/configuration/headers/

# Revalidate the HTML shell after deploys (hashed assets stay cached)
/
  Cache-Control: no-cache

/index.html
  Cache-Control: no-cache
